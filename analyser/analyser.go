package analyser

import (
	"time"
)

// AnalyzeMetrics takes FCP and LCP durations and returns an analysis.
func AnalyzeMetrics(fcp, lcp time.Duration) string {
	// Simulate analyzing the metrics
	analysis := "FCP is within the expected range. LCP is slightly above the expected range."

	return analysis
}
